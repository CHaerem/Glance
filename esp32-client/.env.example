# ESP32 Glance Client Environment Variables
# Copy this file and set your actual values

# WiFi Configuration (REQUIRED)
WIFI_SSID=YourWiFiNetworkName
WIFI_PASSWORD=YourWiFiPassword

# Device Configuration (OPTIONAL)
DEVICE_ID=esp32-001
GITHUB_TOKEN=NONE

# Server Configuration (if different from default)
# API_BASE_URL=http://serverpi.local:3000/api/
# STATUS_URL=http://serverpi.local:3000/api/device-status

# Notes:
# - WIFI_SSID and WIFI_PASSWORD are required for ESP32 to connect
# - DEVICE_ID should be unique if you have multiple ESP32 devices
# - GITHUB_TOKEN is optional and can be left as "NONE"
# - Use 2.4GHz WiFi networks only (ESP32 doesn't support 5GHz)